<Window x:Class="Frontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend"
        mc:Ignorable="d"
        Title="Streaming App" Height="450" Width="800">
    <Grid Margin="10">

        <!-- Profile Section -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,10,0,10">
            <TextBlock Text="Select Profile:" VerticalAlignment="Center"/>
            <ComboBox Name="ProfileComboBox" Width="150" Margin="10,0,0,0"/>
            <Button Content="Create New Profile" Width="150" Margin="10,0,0,0" Click="CreateProfileButton_Click"/>
        </StackPanel>

        <!-- Stream Settings Section -->
        <GroupBox Header="Stream Settings" Margin="0,60,0,60">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Incoming URL -->
                <TextBlock Grid.Column="0" Text="Incoming URL:" VerticalAlignment="Center" Margin="0,10"/>
                <TextBox Grid.Column="1" Name="IncomingUrlTextBox" Margin="5"/>

                <!-- Stream Key -->
                <TextBlock Grid.Column="0" Text="Stream Key:" VerticalAlignment="Center" Margin="0,10"/>
                <PasswordBox Grid.Column="1" Name="StreamKeyTextBox" Margin="5"/>

                <!-- Output Group -->
                <TextBlock Grid.Column="0" Text="Output Group:" VerticalAlignment="Center" Margin="0,10"/>
                <ComboBox Grid.Column="1" Name="OutputGroupComboBox" Margin="5"/>

                <!-- Generate PTS Checkbox -->
                <CheckBox Grid.Column="0" Name="GeneratePTSCheckBox" Content="Generate PTS" VerticalAlignment="Center" Margin="0,10"/>
                
                <!-- Start/Stop Buttons -->
                <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
                    <Button Content="Start Stream" Width="100" Click="StartStreamButton_Click"/>
                    <Button Content="Stop Stream" Width="100" Margin="20,0,0,0" Click="StopStreamButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

    </Grid>
</Window>
